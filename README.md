# SELECT_PRACTICE

Simple echo server with select().

## Usage

```bash
make run
```

then open new terminal and execute followings:

```
$ nc localhost 9999
[type something]
```

Open other terminal and execute nc command like that parallelly, the program can connect to multiple clients.
